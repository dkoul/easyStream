JWT_SECRET=dev-easystream-secret-change-me
PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4000
PORT=4000
# Optional: real ingest. When unset, demo HLS is used so the viewer still works.
# MUX_TOKEN_ID=
# MUX_TOKEN_SECRET=
